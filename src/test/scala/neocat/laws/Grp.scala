package scala

import org.junit.Test
import org.junit.Assert._
/**
import algebra.Group
import neocat.Grp
import instances.ReachCoordinates._

class GrpTest {

    def a: Coordinates = Coordinates(2,5)
    val group = Group[Coordinates]

    @Test def identity(): Unit = {
       assertEquals(Grp.id(group), group)
    }

    @Test def grpMap(): Unit = {
       assertEquals(Grp.map(a)(Group[Coordinates].inverse), Group[Coordinates].inverse(a))
    }

}*/